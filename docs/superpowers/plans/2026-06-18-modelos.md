# Modelos — Implementation Plan (Plan 2 de 4)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar una interfaz común de forecasting y cuatro modelos (seasonal naive, LightGBM, SARIMAX con Fourier, Chronos-Bolt) que producen un pronóstico de 24 h a partir del histórico, todos intercambiables tras el mismo contrato.

**Architecture:** Un paquete `forecasting.models` donde cada modelo es una clase con atributo `name` y método `predict(history, horizon) -> DataFrame`. Un módulo `base` define el contrato y helpers compartidos (índice de horas futuras, columnas de salida). Modelos rápidos y deterministas (naive, lightgbm) se testean a nivel de valores/contrato; modelos pesados (sarimax, chronos) se testean a nivel de **contrato de salida** y se marcan `slow`.

**Tech Stack:** Python, pandas, numpy, lightgbm, statsforecast (Nixtla), chronos-forecasting + torch (CPU). Sigue sobre la fundación del Plan 1 (`store`, `ingest`, `config`).

---

## Contexto para quien ejecuta (leer antes de empezar)

- **Repo:** `C:\Users\asus\Documents\forecasting-demanda-electrica`. Trabaja desde una rama de feature (el controlador la crea). Usa el venv: `.venv/Scripts/python.exe -m pytest ...`. El paquete ya es instalable (`pip install -e .` hecho).
- **Spec:** `docs/superpowers/specs/2026-06-15-forecasting-demanda-electrica-design.md` §5 (Modelado). Léelo: explica el porqué de los 4 niveles.
- **Contrato de modelo (CLAVE):** todo modelo recibe `history` (DataFrame con columnas `period` UTC horaria, ascendente y contigua, y `value` float) y `horizon` (int, normalmente 24). Devuelve un DataFrame con columnas `period` (las próximas `horizon` horas), `model` (== `self.name`) y `prediction` (float, el forecast puntual / mediana). Modelos probabilísticos pueden añadir columnas `q10`/`q90`, pero `prediction` siempre está.
- **Restricción de horizonte:** el forecast es a 24 h. Algunos modelos (LightGBM con lag de 24 h) asumen `horizon <= 24` para que los lags de la ventana futura caigan dentro del histórico sin recursión. Documentar y validar.
- **No mezclar la disciplina de snapshot aquí.** Los modelos consumen una serie limpia `period/value`. La elección de `value_current` vs `value_first_reported` y el corte *as-of* son responsabilidad de la orquestación (Plan 3). Aquí los modelos son agnósticos a eso.
- **Imports incrementales:** `forecasting/models/__init__.py` se mantiene VACÍO hasta la última tarea (Task 5), para que importar un modelo no arrastre dependencias aún no instaladas. Hasta entonces se importa cada modelo por su módulo (`from forecasting.models.naive import SeasonalNaive`).

---

## File Structure

```
src/forecasting/models/
├── __init__.py          # vacío hasta Task 5; luego expone el registry
├── base.py              # contrato (FORECAST_COLUMNS) + helper future_periods
├── naive.py             # SeasonalNaive
├── lightgbm_model.py    # LightGBMForecaster
├── sarimax.py           # SarimaxForecaster (Fourier + AutoARIMA)
└── chronos_model.py     # ChronosForecaster (Chronos-Bolt, zero-shot)
tests/
├── conftest.py          # añade fixture synthetic_hourly_history
├── test_naive.py
├── test_lightgbm.py
├── test_sarimax.py
├── test_chronos.py
└── test_model_registry.py
pyproject.toml           # añade marker pytest "slow"
```

---

## Task 1: Contrato base + SeasonalNaive

**Files:**
- Create: `src/forecasting/models/__init__.py` (empty), `src/forecasting/models/base.py`, `src/forecasting/models/naive.py`
- Modify: `tests/conftest.py`, `pyproject.toml`
- Test: `tests/test_naive.py`

- [ ] **Step 1: Crear `src/forecasting/models/__init__.py` vacío** (un archivo vacío; el registry se llena en Task 5).

- [ ] **Step 2: Añadir marker `slow` y el fixture sintético**

En `pyproject.toml`, dentro de `[tool.pytest.ini_options]`, añadir:
```toml
markers = ["slow: tests que cargan modelos pesados o ajustan modelos lentos"]
```

En `tests/conftest.py` añadir (incluye `import numpy as np` arriba si no está):
```python
import numpy as np


@pytest.fixture
def synthetic_hourly_history():
    """4 semanas de demanda horaria sintética con estacionalidad diaria+semanal.

    Sin ruido → la serie se repite exactamente cada 168 h, así seasonal naive
    es perfecto y los demás modelos tienen una señal limpia que aprender.
    """
    n = 24 * 28  # 4 semanas
    idx = pd.date_range("2025-01-01", periods=n, freq="h", tz="UTC")
    h = np.arange(n)
    daily = 10 * np.sin(2 * np.pi * h / 24)
    weekly = 5 * np.sin(2 * np.pi * h / 168)
    value = 100 + daily + weekly
    return pd.DataFrame({"period": idx, "value": value})
```

- [ ] **Step 3: Escribir el test que falla `tests/test_naive.py`**
```python
import pandas as pd
from forecasting.models.naive import SeasonalNaive
from forecasting.models.base import FORECAST_COLUMNS


def test_naive_output_contract(synthetic_hourly_history):
    model = SeasonalNaive()
    out = model.predict(synthetic_hourly_history, horizon=24)
    assert list(out.columns) == FORECAST_COLUMNS
    assert len(out) == 24
    assert (out["model"] == "seasonal_naive").all()
    # las horas futuras son las 24 siguientes a la última del histórico
    last = synthetic_hourly_history["period"].iloc[-1]
    assert out["period"].iloc[0] == last + pd.Timedelta(hours=1)
    assert out["period"].iloc[-1] == last + pd.Timedelta(hours=24)


def test_naive_repeats_value_one_week_earlier(synthetic_hourly_history):
    model = SeasonalNaive()
    out = model.predict(synthetic_hourly_history, horizon=24)
    values = synthetic_hourly_history["value"].to_numpy()
    n = len(values)
    # prediccion[i] == valor de hace 168 h
    for i in range(24):
        assert abs(out["prediction"].iloc[i] - values[n - 168 + i]) < 1e-9


def test_naive_raises_when_history_too_short():
    short = pd.DataFrame(
        {"period": pd.date_range("2025-01-01", periods=10, freq="h", tz="UTC"),
         "value": range(10)}
    )
    model = SeasonalNaive()
    try:
        model.predict(short, horizon=24)
        assert False, "esperaba ValueError"
    except ValueError:
        pass
```

- [ ] **Step 4: Correr y verificar que falla**

Run: `.venv/Scripts/python.exe -m pytest tests/test_naive.py -v`
Expected: FAIL (`ModuleNotFoundError: No module named 'forecasting.models.base'`).

- [ ] **Step 5: Implementar `src/forecasting/models/base.py`**
```python
"""Contrato común de los modelos de forecasting y helpers compartidos."""
from __future__ import annotations

import pandas as pd

# Columnas mínimas que todo modelo devuelve. Los probabilísticos pueden añadir más.
FORECAST_COLUMNS = ["period", "model", "prediction"]


def future_periods(history: pd.DataFrame, horizon: int) -> pd.DatetimeIndex:
    """Las próximas `horizon` horas (UTC) después de la última del histórico."""
    last = history["period"].iloc[-1]
    return pd.date_range(
        start=last + pd.Timedelta(hours=1), periods=horizon, freq="h"
    )
```
(Nota: `last` es tz-aware UTC, así que `date_range` hereda el tz; no se pasa `tz=` para evitar conflicto.)

- [ ] **Step 6: Implementar `src/forecasting/models/naive.py`**
```python
"""Seasonal naive: el valor de hace exactamente una semana (168 h)."""
import pandas as pd

from forecasting.models.base import FORECAST_COLUMNS, future_periods

WEEK_HOURS = 168


class SeasonalNaive:
    name = "seasonal_naive"

    def predict(self, history: pd.DataFrame, horizon: int) -> pd.DataFrame:
        if len(history) < WEEK_HOURS:
            raise ValueError(
                f"seasonal_naive necesita >= {WEEK_HOURS} h de histórico, "
                f"recibió {len(history)}"
            )
        values = history["value"].to_numpy()
        n = len(values)
        # prediccion[i] = valor de hace una semana respecto a la hora futura i
        preds = [values[n - WEEK_HOURS + i] for i in range(horizon)]
        return pd.DataFrame(
            {
                "period": future_periods(history, horizon),
                "model": self.name,
                "prediction": preds,
            }
        )[FORECAST_COLUMNS]
```

- [ ] **Step 7: Correr y verificar que pasa**

Run: `.venv/Scripts/python.exe -m pytest tests/test_naive.py -v`
Expected: PASS (3 tests).

- [ ] **Step 8: Commit**
```bash
git add src/forecasting/models/__init__.py src/forecasting/models/base.py src/forecasting/models/naive.py tests/conftest.py tests/test_naive.py pyproject.toml
git commit -m "feat(models): contrato base + seasonal naive"
```

---

## Task 2: LightGBMForecaster

**Files:**
- Create: `src/forecasting/models/lightgbm_model.py`
- Modify: `requirements.txt`
- Test: `tests/test_lightgbm.py`

Enfoque **directo sin recursión**: features = calendario de la hora objetivo + `lag_24` (valor 24 h antes) + `lag_168` (valor 168 h antes). Para un horizonte de 24 h, ambos lags de la ventana futura caen dentro del histórico, así que no hace falta predecir recursivamente.

- [ ] **Step 1: Añadir dependencia**

En `requirements.txt` añadir la línea:
```
lightgbm>=4.0
```
Instalar: `.venv/Scripts/python.exe -m pip install "lightgbm>=4.0"`

- [ ] **Step 2: Escribir el test que falla `tests/test_lightgbm.py`**
```python
import numpy as np
import pandas as pd
from forecasting.models.lightgbm_model import LightGBMForecaster
from forecasting.models.base import FORECAST_COLUMNS


def test_lightgbm_output_contract(synthetic_hourly_history):
    model = LightGBMForecaster()
    out = model.predict(synthetic_hourly_history, horizon=24)
    assert list(out.columns) == FORECAST_COLUMNS
    assert len(out) == 24
    assert (out["model"] == "lightgbm").all()
    assert np.isfinite(out["prediction"]).all()
    last = synthetic_hourly_history["period"].iloc[-1]
    assert out["period"].iloc[0] == last + pd.Timedelta(hours=1)


def test_lightgbm_learns_seasonal_signal(synthetic_hourly_history):
    """En una serie limpia y periódica, el forecast debe parecerse al patrón real
    de hace una semana (no exacto como naive, pero correlacionado)."""
    model = LightGBMForecaster()
    out = model.predict(synthetic_hourly_history, horizon=24)
    values = synthetic_hourly_history["value"].to_numpy()
    n = len(values)
    week_ago = np.array([values[n - 168 + i] for i in range(24)])
    corr = np.corrcoef(out["prediction"].to_numpy(), week_ago)[0, 1]
    assert corr > 0.9


def test_lightgbm_raises_when_history_too_short():
    short = pd.DataFrame(
        {"period": pd.date_range("2025-01-01", periods=50, freq="h", tz="UTC"),
         "value": range(50)}
    )
    try:
        LightGBMForecaster().predict(short, horizon=24)
        assert False, "esperaba ValueError"
    except ValueError:
        pass
```

- [ ] **Step 3: Correr y verificar que falla**

Run: `.venv/Scripts/python.exe -m pytest tests/test_lightgbm.py -v`
Expected: FAIL (`ModuleNotFoundError: No module named 'forecasting.models.lightgbm_model'`).

- [ ] **Step 4: Implementar `src/forecasting/models/lightgbm_model.py`**
```python
"""LightGBM con features de calendario + lags conocidos (24 h y 168 h)."""
import lightgbm as lgb
import pandas as pd

from forecasting.models.base import FORECAST_COLUMNS, future_periods

LAG_DAY = 24
LAG_WEEK = 168


def _calendar(idx: pd.DatetimeIndex) -> pd.DataFrame:
    return pd.DataFrame(
        {
            "hour": idx.hour,
            "dayofweek": idx.dayofweek,
            "month": idx.month,
            "is_weekend": (idx.dayofweek >= 5).astype(int),
        }
    )


class LightGBMForecaster:
    name = "lightgbm"

    def __init__(self, **params):
        # verbose=-1 silencia LightGBM; random_state hace el ajuste reproducible.
        self.params = {
            "n_estimators": 200,
            "learning_rate": 0.05,
            "num_leaves": 31,
            "random_state": 0,
            "verbose": -1,
            **params,
        }

    def _build_features(
        self, periods: pd.DatetimeIndex, lookup: dict
    ) -> pd.DataFrame:
        feats = _calendar(periods)
        feats["lag_24"] = [lookup[p - pd.Timedelta(hours=LAG_DAY)] for p in periods]
        feats["lag_168"] = [lookup[p - pd.Timedelta(hours=LAG_WEEK)] for p in periods]
        return feats

    def predict(self, history: pd.DataFrame, horizon: int) -> pd.DataFrame:
        if horizon > LAG_DAY:
            raise ValueError(
                f"lightgbm asume horizon <= {LAG_DAY} (lag_24 conocido sin recursión)"
            )
        if len(history) < LAG_WEEK + 1:
            raise ValueError(
                f"lightgbm necesita > {LAG_WEEK} h de histórico, recibió {len(history)}"
            )
        series = history.set_index("period")["value"]
        lookup = series.to_dict()

        # Filas de entrenamiento: aquellas donde ambos lags existen.
        train_idx = series.index[LAG_WEEK:]
        X_train = self._build_features(train_idx, lookup)
        y_train = series.loc[train_idx].to_numpy()

        model = lgb.LGBMRegressor(**self.params)
        model.fit(X_train, y_train)

        future = future_periods(history, horizon)
        X_future = self._build_features(future, lookup)
        preds = model.predict(X_future)

        return pd.DataFrame(
            {"period": future, "model": self.name, "prediction": preds}
        )[FORECAST_COLUMNS]
```

- [ ] **Step 5: Correr y verificar que pasa**

Run: `.venv/Scripts/python.exe -m pytest tests/test_lightgbm.py -v`
Expected: PASS (3 tests).

- [ ] **Step 6: Correr la suite completa** (asegura que nada se rompió)

Run: `.venv/Scripts/python.exe -m pytest -q`
Expected: PASS.

- [ ] **Step 7: Commit**
```bash
git add src/forecasting/models/lightgbm_model.py tests/test_lightgbm.py requirements.txt
git commit -m "feat(models): LightGBM con calendario y lags 24h/168h"
```

---

## Task 3: SarimaxForecaster (Fourier + AutoARIMA)

**Files:**
- Create: `src/forecasting/models/sarimax.py`
- Modify: `requirements.txt`
- Test: `tests/test_sarimax.py`

Dynamic harmonic regression: estacionalidad múltiple (24 h y 168 h) modelada con **términos de Fourier** como exógenas; el ARIMA (no estacional) modela la autocorrelación de corto plazo. Vía `statsforecast.AutoARIMA`.

- [ ] **Step 1: Añadir dependencia e instalar**

En `requirements.txt`: `statsforecast>=1.7`
Instalar: `.venv/Scripts/python.exe -m pip install "statsforecast>=1.7"`

- [ ] **Step 2: SPIKE de verificación de API (NO se commitea)**

La firma exacta de `statsforecast` varía entre versiones (nombre de la columna de salida, `freq="h"` vs `"H"`, manejo de exógenas en `forecast`). Verifica con un script rápido antes de implementar:
```bash
.venv/Scripts/python.exe - <<'PY'
import numpy as np, pandas as pd
from statsforecast import StatsForecast
from statsforecast.models import AutoARIMA

n = 24*14
ds = pd.date_range("2025-01-01", periods=n, freq="h")
y = 100 + 10*np.sin(2*np.pi*np.arange(n)/24)
train = pd.DataFrame({"unique_id": "PJM", "ds": ds, "y": y})
train["sin1"] = np.sin(2*np.pi*np.arange(n)/24)
train["cos1"] = np.cos(2*np.pi*np.arange(n)/24)
sf = StatsForecast(models=[AutoARIMA(seasonal=False)], freq="h")
fut = pd.date_range(ds[-1] + pd.Timedelta(hours=1), periods=24, freq="h")
Xf = pd.DataFrame({"unique_id": "PJM", "ds": fut,
                   "sin1": np.sin(2*np.pi*np.arange(n, n+24)/24),
                   "cos1": np.cos(2*np.pi*np.arange(n, n+24)/24)})
fc = sf.forecast(df=train, h=24, X_df=Xf)
print(fc.columns.tolist())
print(fc.head())
PY
```
Anota: (a) el nombre de la columna de predicción (típicamente `"AutoARIMA"`), (b) si `freq="h"` es aceptado, (c) si `forecast(df=, h=, X_df=)` funciona con esa firma. Ajusta la implementación del Step 4 a lo que observes. Si la API difiere materialmente de lo asumido, repórtalo antes de seguir.

- [ ] **Step 3: Escribir el test que falla `tests/test_sarimax.py`** (test de contrato; marcado `slow` porque AutoARIMA ajusta)
```python
import numpy as np
import pandas as pd
import pytest
from forecasting.models.sarimax import SarimaxForecaster
from forecasting.models.base import FORECAST_COLUMNS


@pytest.mark.slow
def test_sarimax_output_contract(synthetic_hourly_history):
    model = SarimaxForecaster()
    out = model.predict(synthetic_hourly_history, horizon=24)
    assert list(out.columns) == FORECAST_COLUMNS
    assert len(out) == 24
    assert (out["model"] == "sarimax").all()
    assert np.isfinite(out["prediction"]).all()
    last = synthetic_hourly_history["period"].iloc[-1]
    assert out["period"].iloc[0] == last + pd.Timedelta(hours=1)
    assert out["period"].iloc[-1] == last + pd.Timedelta(hours=24)
```

- [ ] **Step 4: Implementar `src/forecasting/models/sarimax.py`** (ajustar nombres de columna/firma a lo verificado en el spike)
```python
"""SARIMAX vía dynamic harmonic regression: Fourier (24h,168h) como exógenas + AutoARIMA."""
import numpy as np
import pandas as pd
from statsforecast import StatsForecast
from statsforecast.models import AutoARIMA

from forecasting.models.base import FORECAST_COLUMNS, future_periods

SEASONAL_PERIODS = (24, 168)   # diaria y semanal
N_HARMONICS = (3, 3)           # nº de armónicos por período


def _fourier_terms(periods: pd.DatetimeIndex, origin: pd.Timestamp) -> pd.DataFrame:
    """Términos sin/cos para cada período estacional, en función de horas desde origin."""
    hours = ((periods - origin) / pd.Timedelta(hours=1)).to_numpy(dtype=float)
    cols = {}
    for period, k_max in zip(SEASONAL_PERIODS, N_HARMONICS):
        for k in range(1, k_max + 1):
            cols[f"sin_{period}_{k}"] = np.sin(2 * np.pi * k * hours / period)
            cols[f"cos_{period}_{k}"] = np.cos(2 * np.pi * k * hours / period)
    return pd.DataFrame(cols)


class SarimaxForecaster:
    name = "sarimax"

    def predict(self, history: pd.DataFrame, horizon: int) -> pd.DataFrame:
        origin = history["period"].iloc[0]
        hist_idx = pd.DatetimeIndex(history["period"])
        future = future_periods(history, horizon)

        X_hist = _fourier_terms(hist_idx, origin)
        X_fut = _fourier_terms(future, origin)

        # statsforecast usa `ds` naive (sin tz); guardamos el period tz-aware para la salida.
        train = pd.DataFrame(
            {"unique_id": "PJM", "ds": hist_idx.tz_localize(None), "y": history["value"].to_numpy()}
        )
        train = pd.concat([train, X_hist], axis=1)

        X_df = pd.DataFrame({"unique_id": "PJM", "ds": future.tz_localize(None)})
        X_df = pd.concat([X_df, X_fut], axis=1)

        sf = StatsForecast(models=[AutoARIMA(seasonal=False)], freq="h")
        fc = sf.forecast(df=train, h=horizon, X_df=X_df)

        return pd.DataFrame(
            {"period": future, "model": self.name, "prediction": fc["AutoARIMA"].to_numpy()}
        )[FORECAST_COLUMNS]
```

- [ ] **Step 5: Correr el test (marcado slow → hay que pedirlo explícito)**

Run: `.venv/Scripts/python.exe -m pytest tests/test_sarimax.py -v -m slow`
Expected: PASS (1 test). Si falla por nombre de columna o `freq`, ajusta según el spike del Step 2 y reintenta. (Puede tardar varios segundos por el ajuste de AutoARIMA.)

- [ ] **Step 6: Confirmar que la suite normal sigue verde** (los tests slow se omiten por defecto)

Run: `.venv/Scripts/python.exe -m pytest -q`
Expected: PASS, con los tests `slow` deseleccionados.

- [ ] **Step 7: Commit**
```bash
git add src/forecasting/models/sarimax.py tests/test_sarimax.py requirements.txt
git commit -m "feat(models): SARIMAX con Fourier (24h/168h) y AutoARIMA"
```

---

## Task 4: ChronosForecaster (Chronos-Bolt, zero-shot)

**Files:**
- Create: `src/forecasting/models/chronos_model.py`
- Modify: `requirements.txt`
- Test: `tests/test_chronos.py`

Modelo fundacional preentrenado; predice zero-shot y da cuantiles. Corre en CPU. Dependencia pesada (torch).

- [ ] **Step 1: Añadir dependencias e instalar (torch CPU)**

En `requirements.txt`:
```
torch>=2.2
chronos-forecasting>=1.4
```
Instalar (CPU; en Windows el wheel por defecto de torch es CPU):
```bash
.venv/Scripts/python.exe -m pip install "torch>=2.2" "chronos-forecasting>=1.4"
```

- [ ] **Step 2: SPIKE de verificación de API (NO se commitea)**

Confirma el tipo de retorno de `predict_quantiles` para Chronos-Bolt en la versión instalada (puede ser una tupla `(quantiles, mean)` o un único tensor). Usa el modelo `tiny` para que sea rápido:
```bash
.venv/Scripts/python.exe - <<'PY'
import torch
from chronos import BaseChronosPipeline
p = BaseChronosPipeline.from_pretrained("amazon/chronos-bolt-tiny", device_map="cpu", torch_dtype=torch.float32)
ctx = torch.arange(200, dtype=torch.float32)
res = p.predict_quantiles(context=ctx, prediction_length=24, quantile_levels=[0.1, 0.5, 0.9])
print(type(res))
if isinstance(res, tuple):
    q, m = res
    print("quantiles", q.shape, "mean", m.shape)
else:
    print("tensor", res.shape)
PY
```
Anota la forma exacta. Lo esperado para `BaseChronosPipeline`: una tupla `(quantiles, mean)` con `quantiles` de forma `(1, 24, 3)`. Ajusta el Step 4 si difiere (p.ej. si es un solo tensor, indexa `res[0]`).

- [ ] **Step 3: Escribir el test que falla `tests/test_chronos.py`** (contrato; `slow` porque descarga y corre el modelo)
```python
import numpy as np
import pandas as pd
import pytest
from forecasting.models.chronos_model import ChronosForecaster
from forecasting.models.base import FORECAST_COLUMNS


@pytest.mark.slow
def test_chronos_output_contract(synthetic_hourly_history):
    # modelo tiny para rapidez en test
    model = ChronosForecaster(model_id="amazon/chronos-bolt-tiny")
    out = model.predict(synthetic_hourly_history, horizon=24)
    for col in FORECAST_COLUMNS:
        assert col in out.columns
    assert {"q10", "q90"}.issubset(out.columns)
    assert len(out) == 24
    assert (out["model"] == "chronos").all()
    assert np.isfinite(out["prediction"]).all()
    # bandas coherentes: q10 <= prediction <= q90 (la mayoría de las horas)
    assert (out["q10"] <= out["prediction"] + 1e-6).mean() > 0.9
    assert (out["prediction"] <= out["q90"] + 1e-6).mean() > 0.9
    last = synthetic_hourly_history["period"].iloc[-1]
    assert out["period"].iloc[0] == last + pd.Timedelta(hours=1)
```

- [ ] **Step 4: Implementar `src/forecasting/models/chronos_model.py`** (ajustar al retorno verificado en el spike)
```python
"""Chronos-Bolt (Amazon): transformer preentrenado, forecasting zero-shot probabilístico."""
import pandas as pd
import torch
from chronos import BaseChronosPipeline

from forecasting.models.base import FORECAST_COLUMNS, future_periods

QUANTILE_LEVELS = [0.1, 0.5, 0.9]


class ChronosForecaster:
    name = "chronos"

    def __init__(self, model_id: str = "amazon/chronos-bolt-small", context_length: int = 512):
        self.model_id = model_id
        self.context_length = context_length
        self._pipeline = None  # carga perezosa: solo al primer predict

    def _load(self):
        if self._pipeline is None:
            self._pipeline = BaseChronosPipeline.from_pretrained(
                self.model_id, device_map="cpu", torch_dtype=torch.float32
            )
        return self._pipeline

    def predict(self, history: pd.DataFrame, horizon: int) -> pd.DataFrame:
        pipeline = self._load()
        values = history["value"].to_numpy()[-self.context_length:]
        context = torch.tensor(values, dtype=torch.float32)

        quantiles, _mean = pipeline.predict_quantiles(
            context=context, prediction_length=horizon, quantile_levels=QUANTILE_LEVELS
        )
        q = quantiles[0].numpy()  # forma (horizon, 3): [q10, q50, q90]

        return pd.DataFrame(
            {
                "period": future_periods(history, horizon),
                "model": self.name,
                "prediction": q[:, 1],   # mediana
                "q10": q[:, 0],
                "q90": q[:, 2],
            }
        )[FORECAST_COLUMNS + ["q10", "q90"]]
```

- [ ] **Step 5: Correr el test (slow; la primera vez descarga el modelo tiny)**

Run: `.venv/Scripts/python.exe -m pytest tests/test_chronos.py -v -m slow`
Expected: PASS (1 test). Requiere conexión a internet la primera vez (HuggingFace).

- [ ] **Step 6: Confirmar suite normal verde**

Run: `.venv/Scripts/python.exe -m pytest -q`
Expected: PASS (los slow deseleccionados).

- [ ] **Step 7: Commit**
```bash
git add src/forecasting/models/chronos_model.py tests/test_chronos.py requirements.txt
git commit -m "feat(models): Chronos-Bolt zero-shot con bandas de cuantiles"
```

---

## Task 5: Registry de modelos + test de contrato cruzado

**Files:**
- Modify: `src/forecasting/models/__init__.py`
- Test: `tests/test_model_registry.py`

Expone los modelos agrupados según dónde corren (spec §5): el loop diario (naive, lightgbm, chronos) y el job semanal (sarimax). Un test parametrizado verifica que TODOS cumplen el contrato sobre la misma entrada.

- [ ] **Step 1: Escribir el test que falla `tests/test_model_registry.py`**
```python
import numpy as np
import pytest
from forecasting import models
from forecasting.models.base import FORECAST_COLUMNS


def test_registry_groups():
    daily = {m.name for m in models.daily_models()}
    weekly = {m.name for m in models.weekly_models()}
    assert daily == {"seasonal_naive", "lightgbm", "chronos"}
    assert weekly == {"sarimax"}
    assert {m.name for m in models.all_models()} == daily | weekly


# Modelos rápidos y deterministas: contrato verificado siempre.
@pytest.mark.parametrize("model_name", ["seasonal_naive", "lightgbm"])
def test_fast_models_obey_contract(model_name, synthetic_hourly_history):
    model = next(m for m in models.all_models() if m.name == model_name)
    out = model.predict(synthetic_hourly_history, horizon=24)
    for col in FORECAST_COLUMNS:
        assert col in out.columns
    assert len(out) == 24
    assert (out["model"] == model_name).all()
    assert np.isfinite(out["prediction"]).all()
```

- [ ] **Step 2: Correr y verificar que falla**

Run: `.venv/Scripts/python.exe -m pytest tests/test_model_registry.py -v`
Expected: FAIL (`AttributeError: module 'forecasting.models' has no attribute 'daily_models'`).

- [ ] **Step 3: Implementar el registry en `src/forecasting/models/__init__.py`**
```python
"""Registry de modelos de forecasting, agrupados por cadencia de ejecución (spec §5)."""
from forecasting.models.chronos_model import ChronosForecaster
from forecasting.models.lightgbm_model import LightGBMForecaster
from forecasting.models.naive import SeasonalNaive
from forecasting.models.sarimax import SarimaxForecaster


def daily_models():
    """Modelos rápidos/robustos que corren en el loop diario."""
    return [SeasonalNaive(), LightGBMForecaster(), ChronosForecaster()]


def weekly_models():
    """Modelos costosos, aislados en un job semanal (no tumban el loop diario)."""
    return [SarimaxForecaster()]


def all_models():
    return daily_models() + weekly_models()
```

- [ ] **Step 4: Correr y verificar que pasa**

Run: `.venv/Scripts/python.exe -m pytest tests/test_model_registry.py -v`
Expected: PASS (3 tests: registry + 2 parametrizados).

- [ ] **Step 5: Correr TODA la suite, incluyendo slow** (verificación end-to-end de los 4 modelos)

Run: `.venv/Scripts/python.exe -m pytest -q -m "slow or not slow"`
Expected: PASS (todos: fundación del Plan 1 + modelos rápidos + sarimax + chronos).

- [ ] **Step 6: Commit**
```bash
git add src/forecasting/models/__init__.py tests/test_model_registry.py
git commit -m "feat(models): registry agrupado por cadencia + test de contrato cruzado"
```

---

## Self-Review

**1. Spec coverage (spec §5):**
- Nivel 0 Seasonal naive → Task 1. ✅
- Nivel 1 SARIMAX + Fourier + AutoARIMA, aislado (weekly) → Task 3 + registry `weekly_models` (Task 5). ✅
- Nivel 2 LightGBM + features de calendario → Task 2. ✅
- Nivel 3 Chronos-Bolt zero-shot, probabilístico (cuantiles) → Task 4. ✅
- Comparación justa con misma metodología → contrato común `predict(history, horizon)` + test cruzado (Task 5). La comparación numérica/backtest es Plan 3 (no aquí). ✅
- Bandas de incertidumbre de Chronos en v1 → columnas `q10`/`q90` (Task 4). ✅

**2. Placeholder scan:** sin TBD/TODO. Los dos *spikes* (Tasks 3 y 4) son verificación de API explícita, no placeholders: el código del adaptador está completo y solo se ajusta si la versión instalada difiere. ✅

**3. Type/firma consistency:**
- Todos los modelos: atributo `name` (str) + `predict(history, horizon) -> DataFrame` con `FORECAST_COLUMNS`. ✅
- `future_periods(history, horizon)` definido en `base` (Task 1) y usado por naive/lightgbm/sarimax/chronos. ✅
- `FORECAST_COLUMNS` importado de `base` en todos los modelos y tests. ✅
- Registry (`daily_models`/`weekly_models`/`all_models`) en `__init__` (Task 5) coincide con los nombres usados en `test_model_registry`. ✅
- `__init__.py` vacío hasta Task 5 → las Tasks 1-4 importan por módulo, evitando arrastrar deps no instaladas. ✅
