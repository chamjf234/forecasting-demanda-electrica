# Dependencias base / dashboard (livianas).
# El dashboard de Streamlit solo necesita estas: lee los parquet versionados y
# calcula vistas/métricas con pandas. Streamlit Cloud instala este archivo.
# Las librerías pesadas de modelos están en requirements-models.txt (CI/cron/local).
pandas>=2.2
numpy>=1.26
pyarrow>=16.0
requests>=2.32
python-dotenv>=1.0
pyyaml>=6.0
streamlit>=1.36
pytest>=8.0
